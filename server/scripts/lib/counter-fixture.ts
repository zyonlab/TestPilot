import {createServer} from 'node:http';
/** Exact counter fixture shared by first parity and subsequent frozen repetitions. */
export function counterFixture(){let count=0;return createServer((req,res)=>{if(req.url==='/state'){res.setHeader('content-type','application/json');res.end(JSON.stringify({count}));}else if(req.url==='/increment'){count++;res.end(String(count));}else if(req.url!=='/'){res.statusCode=404;res.end();}else {count=0;res.setHeader('content-type','text/html');res.end('<!doctype html><html><title>Counter</title><body style="font:28px system-ui;padding:72px"><h1>Counter</h1><p>count: <span id="count">0</span></p><button style="font:24px system-ui;padding:20px" onclick="fetch(\'/increment\',{method:\'POST\'}).then(r=>r.text()).then(n=>document.getElementById(\'count\').textContent=n)">Increment</button></body></html>');}});}
