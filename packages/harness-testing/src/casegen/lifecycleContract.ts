/** Shared by constrained decoding and host unit contracts. Product details come from project sources. */
export const LIFECYCLE_INSTRUCTIONS = `Every new case includes lifecycle version 1. Completeness is explicit obligations, never a fixed or average step count.
Use mode read-only for a genuinely read-only or rejected operation, explain rationale, bind sourceRefs to the case sources and supports to $expected or assertion ids; give deterministic same-screen baseline checks covering every exact precondition statement, resources:[] and cleanup:[]. Rejection assertions must prove that no resource was created.
For controlled resources, keep actions in the existing steps and postSteps. Each resource has id, sourceRef, identity containing the literal runner placeholder \${env.TP_LIFECYCLE_ID}, establishAfterStep (1-based), established and ownership checks. The creation step and cleanup postStep must explicitly name the exact identity template. Each cleanup has id, resourceId, postStep (1-based), verified check. Cleanup is mandatory compensation on failure too. Checks are {statement,checks:[{kind:"screen",statement,oracle:{kind:"text"|"noText",value}}]}; never a model declaration of verified state. Ownership evidence must name the identity and project-provided owner/context. The runner checks baseline absence of its fresh identity before actions. Only resources created by this execution are supported; existing shared mutable fixtures require readiness blocked with a reason, not an invented ownership claim. Recipes remain none/ui-only. No SUT API calls. Missing project evidence stays blocked/unknown. Preparers cannot weaken the reviewed contract or move its action bindings.`;
const strings={type:'array',items:{type:'string',minLength:1}} as const;
const check={type:'object',additionalProperties:false,properties:{statement:{type:'string'},checks:{type:'array',minItems:1,items:{type:'object',additionalProperties:false,properties:{kind:{type:'string',enum:['screen']},statement:{type:'string'},oracle:{type:'object',additionalProperties:false,properties:{kind:{type:'string',enum:['text','noText']},value:{type:'string'}},required:['kind','value']}},required:['kind','statement','oracle']}}},required:['statement','checks']} as const;
export const LIFECYCLE_JSON_SCHEMA={type:'object',additionalProperties:false,properties:{version:{type:'integer',enum:[1]},mode:{type:'string',enum:['read-only','controlled']},rationale:{type:'string'},sourceRefs:{...strings,minItems:1},supports:{...strings,minItems:1},baseline:{type:'array',minItems:1,items:check},resources:{type:'array',items:{type:'object',additionalProperties:false,properties:{id:{type:'string'},sourceRef:{type:'string'},identity:{type:'string'},establishAfterStep:{type:'integer',minimum:1},established:check,ownership:check},required:['id','sourceRef','identity','establishAfterStep','established','ownership']}},cleanup:{type:'array',items:{type:'object',additionalProperties:false,properties:{id:{type:'string'},resourceId:{type:'string'},postStep:{type:'integer',minimum:1},verified:check},required:['id','resourceId','postStep','verified']}}},required:['version','mode','rationale','sourceRefs','supports','baseline','resources','cleanup']} as const;
