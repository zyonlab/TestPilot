# 09 · 执行目标与接手指南

这一页只写**现在成立**的东西：目标、范围、怎么接着干、怎么留记录。架构看 [00](00-架构.md)，数据形状看 [01](01-数据契约.md)，安装看 [10](10-安装与诊断.md)，宿主接入看 [14](14-Claude-Code与Codex接入实操.md)。
2026-09-16 之前的交接记录、实验报告、任务台账与设计提案都挪进了 [history/](history/)，只作追溯，**不代表现状**；其中的数字只在当时成立，有的后来被更正过（例如 2026-09-16 的成本对照，见交接日志）。

## 1. 目标

把需求材料（spec）或对被测网站的探索（explore），变成人能复核、能执行的**端到端 UI 测试**：
产品模型 → 模块树（人冻结）→ 用户故事 → 文本用例 → 设计门禁 → 人工复核批准 → 编译成 Midscene 动作代码 → 浏览器执行 → 导出。

几条不变的原则（细节与出处在仓库根 `CLAUDE.md`）：

- **判决从屏幕读**，不调被测站的接口。
- **领域内容是项目数据**（规则包、领域参考、环境画像），代码里不写死领域词；`pnpm check:domain-neutral` 守着。
- **人把关的两处不交给模型**：模块树冻结、用例批准。自愈不许改 oracle。
- **安全只有一张禁止名单** `guard.denyHosts`；不可逆步骤默认放行（它们是被测功能的一部分），运营方用 `GUARD_STRICT=1` 整机拦。

## 2. 当前阶段：初步交付（开源发布）

| 项 | 定了什么 |
|---|---|
| 形式 | 开源，MIT；新建公开仓库、**不带提交历史**，从整理后的快照做第一个提交；现有私有仓库原样保留 |
| 入口 | Web（:5300，API :5301）+ Claude Code。Web 发起的生成默认由本机 Claude Code 规划；Codex、Penguin 标实验性 |
| 验收被测对象 | Hyperliquid 测试网（`https://app.hyperliquid-testnet.xyz/trade`；主网在禁止名单上）。2026-09-18 起只剩这一个 |
| 冻结不做 | 自进化、经验与反例库、子 agent 并行、去不去掉 Penguin、Gold 与记分板、论文研究线。代码里相关模块还在，但不在交付范围 |

新需求先问一句：初步交付需不需要它。

## 3. 现状（2026-09-16）

- **全流程跑通过**，由 Claude Code 规划：Hyperliquid 测试网从探索到门禁通过，并走完复核、编译、执行、导出。（2026-09-15 也在一个本地自建的待办应用上跑通过同一条链路，那条线与它的数据已在 2026-09-18 清掉，记录见 history。）
- **每一跑可以量四个节点**：`pnpm exec tsx server/scripts/measure-run.ts <runId> <projectId>`（在 `server/` 下运行时路径写 `scripts/measure-run.ts`）。产品结构、用户故事、文本用例、用例代码四张表，指标全部取自服务端已有的机检（`checkModulePlan` / `checkStories` / `runGate`）。
- **单元循环的材料已瘦身**：领域参考全文只在 `load_run_instructions` 里发一次；词表、易变读数、角色、规则包仍随每个单元发——拿走词表会让故事从「用户做什么」退化成「屏幕上有什么」，这是实测出来的。
- 算 Claude 流水的 token 与成本，要**按 `message.id` 去重**（同一条消息会拆成多行，每行都带同一份 usage）。
- **生成内容有了判据**：`judge`（几句是/否条件，采样多次按统计口径判）。golden 集 `fixtures/judge-golden/`，真实评测 `server/scripts/eval-judge.ts --real`。
- **失败会沉淀**：执行里判定失败、或带理由被驳回的用例自动进回归候选，人批准后进项目回归集（`server/src/regressionCandidates.ts`）。
- **每跑有归因报表**：`GET …/workflow-runs/:runId/report` / 宿主工具 `tp_run.report` / `measure-run.ts`，把信号按固定规则归到模型、上下文、工具、工作流、用例、产品六层。

## 4. 下一步

按优先级：

1. **公开快照**：确定要进公开仓库的文件清单。建议只带代码、`plugins/`、`fixtures/`、`README.md` / `README.en.md`、`LICENSE`、以及 `docs/v3/` 下的 00 / 01 / 09 / 10 / 14；不带 `docs/v3/history/`、`docs/v3/evidence/`、`docs/reports/`、`docs/archive/`、`benchmark/` 里的私有标注（gold、human-labels、held-out、rubric）。发布前扫一遍本机路径、钱包地址与密钥形状。建公开仓库、push、发布都要当轮确认。
2. **交付前要补的产品缺口**（都是实测撞到的）：
   - 起跑话术没告诉规划器「被模块冻结门禁挡住时停下来报告」，它会在后续阶段上白烧几轮。
   - 规划器还在跑时调 `/resume` 返回 409 `workflow_not_resumable`；冻结后它会自己接着跑，但接口的说法容易误导。
   - Claude Code 两种接入方式各缺一样：`--plugin-dir` 生成的 `.mcp.json` 不带模型配置文件路径；项目级安装不装门禁 hook。
   - `server/package.json` 里还有分叉主网之类的 Web3 脚本，公开前要清。
   - `pnpm setup` / `pnpm doctor` 会跑到 pnpm 自带的同名命令上（`pnpm setup` 会改 shell 配置），文档与 `--help` 里一律写 `node scripts/testpilot-setup.mjs …`；script 名可考虑改掉。
   - `server/.env.example` 里 `TP_WEB_ORIGINS` 的注释还写着 reviewer sessions（已取消）。
   - 代码内部：g2 代码包的 `code[]` 带 `entryUrl`，`CodeCaseSchema` 里没有这个字段；`contracts.ts` 顶部自称「唯一 zod 落点」已不成立；`src/lib/audit.ts` 的类型还照旧 `Decision` 抄，而人工批准的真源已是账本批准事件（见 01 §6、§12）。
3. **这一轮留下的**：
   - 回归集还不会被执行自动带上，现在只是一份清单。
   - 由失败或驳回理由**生成新用例**没做（要调模型，而且写出来还得走一遍完整复核）。
   - `judge` 的「多次采样能暴露不稳定」还没被数据检验：golden 9 条都界限分明，54 次采样零分歧。要加模棱两可的题。
   - 既有不一致：`tierOf` 对 `none` 返回 1，而注释说算 3；`testpilot-design/REFERENCE-oracle.md` 的「第六种 api」一节仍写着资金类结果「必须」用接口判据，和「判决从屏幕读」冲突（门禁会拦，但宿主读到的说法是反的）。
4. **验证还不够的地方**：单元瘦身的收益（用例段约 −23%）与故事质量回升都只各跑了一次；「动作型准则占比」只能在模块树相同的两跑之间比较。

## 5. 验收命令

```bash
pnpm typecheck && pnpm test
pnpm check:drift            # 两臂提示词逐条认领、skill 摘要与宿主副本一致
pnpm check:host-parity      # 宿主入口覆盖 UI 操作
pnpm check:domain-neutral   # 产品代码与 skill 里没有写死的领域内容
```

改了 `packages/harness-testing/src/casegen/prompts.ts` 或 `plugins/testpilot/skills/**`：跳 `plugins/testpilot/plugin.json` 的 `skillVersions`，再跑 `node scripts/build-claude-plugin.mjs` 与 `node scripts/build-codex-plugin.mjs`。

## 6. 怎么留记录

每次停下之前，在 §7 **最上面**加一条，写清：

- 先写失败与出乎意料的，再写改了什么；
- 关键文件、验证命令与结果；
- 数字带来源（哪一跑、哪个脚本、怎么算的）；数字后来被推翻了，就在新的一条里明写更正；
- 还需要人决定的事、外部条件、样本限制。

实验报告与长篇对照放进 `history/` 或 `evidence/`，这里只留结论与链接。不要用硬编码分数、mock 的成功或进度百分比代替真实验收。本页可能公开，不写本机路径、钱包地址、密钥。

## 7. 最近交接点

### 2026-09-23：全部工作区修改的合并验收

- 我在全量测试中发现旧回归候选夹具缺少 execution readiness，补齐夹具的显式 ready 状态，没有放宽生产门禁。用户确认本次提交包含全部现有修改，包括此前评测、Gold、界面、材料及历史报告。
- 我修正五处 skill 领域词、同步故事流程与同屏数值判据说明，四个 skill 版本更新为 2026-09-23.1，重新生成 Claude Code/Codex 副本及摘要；此前的 domain-neutral 与 drift 失败已解决。
- 本地验收：全量测试 181 个文件、1535 项通过；typecheck、build、26 项 hook 测试、7 项脚本测试、drift、domain-neutral、host-parity、冻结重放均通过。宿主覆盖仍是 188/192，四条 todo 保留。CI 维持关闭，本次结果来自本地命令。
- 公开提交检查覆盖新增及修改文件和证据 ZIP；gitleaks 的 61 项匹配逐项确认均为 JSON 中的 SHA-256 文件摘要，并非凭据。未改写历史证据或增加全局扫描豁免。
- `git diff --cached --check` 在历史报告的原始页面文本、错误回执中报告尾部空白；保留这些原始文本。排除 `docs/reports` 后检查通过，未把全量空白检查记为通过。
- 此次合并不代表真实业务准备链路已经验收；受控 fixture、参数化方法、公共 helper 导出和 Offline 问题继续按下方交接推进。

### 2026-09-23：执行准备的事实检查与公共方法基础

- 我确认前置条件全部走截图断言、整组断言相等比较误拦辅助校验；另外修复纯视觉用例漏执行末尾逐条断言的问题。
- 新增带来源的 environment/screen/unknown 前置检查、独立辅助校验、低影响 recipe 与不可变逐轮经验快照。真实独立回执才可晋升；作用域隔离、负面撤销、过期、held-out 隔离和已分发版本追踪已接入。未将测试结果计入真实业务通过数。
- 验证：全仓 typecheck；执行器包 642 项；服务端准备／经验／执行／路由 44 项；最后执行器定向复验 14 项通过。独立测试网浏览器诊断核对 origin、注入钱包和链配置，转发模型调用 0；认证保留 unknown。就绪后截图显示 Offline、交易主体未加载，因此未进行完整业务验收。原批次仍 interrupted，4 verified，未恢复运行。
- host parity 补录原有四条缺失路由为 todo 后通过（188/192）；domain-neutral 仍有五处既有 skill 文案失败。本轮未覆盖这些已有修改。
- 受控 fixture、跨步骤历史账本、参数化方法及公共 helper 导出未实现。真实业务代表用例与公共方法仍待完整联调。详见 [实现、证据与后续工作](../reports/preparation-contracts-2026-09-23.md)。


### 2026-09-20：按用户要求清空动态业务数据

- 旧运行数据除了主数据库，还在多个 .data 测试/历史目录、两处 Midscene 报告目录、项目 Gold 和 scoreboard 文件。停止 API/runner 后，将这些路径移到 `/Users/admin/.codex/backups/testpilot-data-20260920-093806`，manifest.json 记录恢复方式；未永久删除备份。
- 保留全局 model.json、secret.key、环境/源码配置、固定 fixtures、非项目 Gold 及历史文档报告；项目关联 Gold 两套移出，历史 casegen scoreboard 重置 entries: []。
- 重启 API 5301；项目、测试用例、运行、工作流产物、复核记录、基线、评测、提升实验和评分记录均为 0。归档固定 benchmark catalog 2 项保留，启动事件会重新产生。浏览器刷新后显示未选择项目及没有评测记录。
- 前述交接记录中的 run/project/study ID 现只对应备份和历史报告，不能再作为当前在线数据使用。


### 2026-09-20：迭代评测拆分三个页签

- 用户反馈平铺不友好：项目对照、Harness 实验、高级图实验改为互斥页签，项目/图历史按各自范围显示，切换保存各自选中的结果，A/B 表单与 Harness 评审草稿保留。
- 页签支持左右键、Home/End、ARIA tab/tabpanel 与 URL evalTab 参数；刷新后仍处于指定页签。生命周期随对应页签显示。
- 验证：app 类型检查、2072 条三语词条、生产构建通过；浏览器验证项目→Harness→键盘右键高级图实验，历史隔离及禁用准入状态。此前本地服务停止，已恢复 UI 5310/API 5301；未触发实验或修改 Gold。


### 2026-09-20：Gold / 迭代评测生命周期可视化

- 发现：原 Gold 将“已保存”误当“已复核”，README 检查说明一直渲染未勾选；公共组件实验与项目对照混排。已修正状态含义，未将诊断完成当成选版完成。
- 产品：新增 Lifecycle 组件；Gold 五阶段及条目/复核/拆分数量、冻结指纹与下一步；迭代评测四阶段、当前项目 Gold 下拉、项目历史、自动回填已有对照 A/B；公共 Harness 和图定义改为可展开独立区。公共实验显示协议、执行、封存、评审四阶段。
- 数据：Gold 只读状态新增 projectId、reviewIssues（校验凭据内容哈希与 stale）；UI 筛选当前项目并消费服务端复核校验。未修改冻结 Gold、评测结果或提升证据。
- 验证：app/server TypeScript、三语 i18n、生产构建通过；Gold/registered-eval 10 项测试通过（新增伪造复核凭据不可显示就绪的投影测试）；机械 UI 检查无发现。真实浏览器验证评测→Gold 导航、2/2 复核、dev 1/heldout 1、冻结锁定与下一步，检查窄屏布局。
- 边界：生命周期是当前数据条件投影，不是完整事件时间线；项目诊断不能选版，导出组件实验不替代项目验收。Gold 表格仍保留高级字段编辑。


2026-09-20 · 已形成受控导出层提升证据（非整机 / 模型泛化结论）。用户明确要求继续完善并委托代理执行评审，覆盖此前“冻结不做自进化 / 评测”的范围约束。先遇到两次浏览器准备失败、一次真实模型 JSON 契约不合格，均保留。首次 144 次实验发现历史导出器丢失中间断言；复核又修复无机器判据时最终附加屏幕断言被跳过。最终从产品 UI 启动 study-a615981b-17d2-4beb-ac03-b1be7fb65e9a，8 条命名契约 × 正常/中间故障/最终故障 × 两臂 × 3 次 = 144 次真实 Chrome 执行。冻结同一模型生成输入，对照真实 d2e62ce 导出模块与当前导出模块，共享依赖固定。稳定检出故障场景 8/16→16/16；两臂各 24 次正常执行零误报；不可观测、条件内不一致及丢失既有检出均为零；保留验证 4/8→8/8。模型两次调用、1884 tokens；执行用共同的确定性按钮适配器，不代表 Midscene 动作规划提升。八条契约共用两步状态机和两类故障机制，保留集由同一代理设计，不能说独立盲测或统计显著。

实现：`server/src/evidenceStudy/` 固定协议、冻结模型输入、注入校准、真实导出/执行、评分、原始证据、锁与恢复、封存和评审；`fixtures/export-contract-v1/` 是组件任务，不混入 Gold；`EvidenceStudy.tsx` 接入 Evals / Scoreboard，host registry 同步。用户委托评审已在 UI 以 `user-delegated-agent-review` 提交 accept-bounded，绑定 633 文件摘要 `6a36b59035744ea4a21ef2f8bef6db0704546ae0559c8b3f8027d3d2f593d8b1`；不是独立人工标注，不授权对外发布。`verify-evidence-study.ts` 校验原始文件并重算分数；`build-evidence-report.py` 生成报告及证据 ZIP。最终报告：`docs/reports/harness-improvement-evidence-2026-09-20.html`。

验证：全量 Vitest 1459 通过，Node MCP 4、hooks 26、脚本 7；typecheck/build、drift、domain-neutral、i18n、host parity 188/188（UI 专属12）通过。曾发现运行生成的 Playwright spec 被 Vitest 误收集，已在 server/vitest.config.ts 排除 .data 运行目录，保留全部单测来源。两种原生 MCP 配置实读同一 study 成功，仅证明读接口对齐，不代表全宿主性能等价。导出层以外仍需任务生成质量、真实网站、独立隐藏集等实验；没有修改既有 Gold，也没有提交或推送。


2026-09-20 接续核验（真实执行完成于 2026-09-19）：用户明确委托代办人工步骤，已在 UI 冻结模块、逐项检查并批准 Codex 的 6 条用例、复核冻结 ui-trade-20260919（33b232928b0b3d4e，1 dev + 1 heldout）；这是用户委托助手复核，不是独立人工标注。Codex v2 run-14c808d9-6019-4088-b95e-a256f4c691c7 经门禁 0→1 后定稿。首轮默认小视口 3 通过/3 失败；1440×1000 第二轮 exec-50df03f5-886d-4a13-9e6f-5f3ec3cfeabd 为 4 通过/2 不可观测；修复 oracle:none 分派后 exec-235ead27-e1db-4f9b-b7cc-2cb26c5a12f4 定向复测 C-01/C-02，两条通过（结果 rev-1f939ba2-b448-44db-9e0a-8322f55c13e1）。六条最新结果均通过，不能称修复后单批全量 6/6。全部仅 /trade 页内、未连接账户或交易。

Claude v2 run-127d04ed-878f-47a5-91a3-b30abea921f8 生成 16 条，补 acRefs 后门禁 0→1 定稿；未批准执行其 16 条。UI 对照 eval-2f52d798-5031-4a50-a969-99933d8a3931 成功，两个插件读取同一记录成功，记分板诊断区已展示。单个开发项规则覆盖 Codex 0%、Claude 100%，不是行为通过率/选版结论；未改 Gold 追分。旧 Claude v1 已通过 UI 停止，失败提案保留。

落地修复：Stop hook 合法模块等待；exec/run.ts 把显式 none 交给屏幕判定且失败仍失败；export/decisionDelivery 从批准修订取 assertions/afterStep、携带环境视口，修正文件头导致的导出哈希失配；Gold 冻结态勾选显示审核凭证；修复 checkpoint 测试自身的运行竞争。验收：pnpm typecheck、pnpm test 全仓 1451 条通过；hooks 26、scripts 7；host-parity 184/184、drift、domain-neutral、i18n、build 通过。28 文件导出含 6 测试/25 断言，校验器与 Playwright --list 通过，未将加载检查当成独立执行。尚需扩大 Gold、重复/留出/变异评测；没有宣称交易业务全覆盖。完整报告：../reports/delegated-full-flow-2026-09-19.html 与同名 JSON。未提交/推送。

2026-09-19 继续单页面实走：通过真实 Codex MCP 启动 run-6751f6eb-83c5-4c6a-8599-feadfe6073e4，已绑定 scope 与 /trade 单页材料；模块 rev-cf5f1312-a8ad-4527-af8a-e89f5b0365d8 包含行情风险、委托准备、当前页账户状态三组，findings=[]，已在 UI 展开冻结入口并请求用户确认。等待期间用浏览器实测 Limit、Trades、Connect 页内交互，URL 均保持 /trade，不算生成用例执行成绩。修复 penguinRun 中 Codex 错标 Claude Code/trace 路径，以及人工等待时缺 gate.json 被附为失败错误的问题（原始诊断另存）。定向 5 测试与 server typecheck 通过。尚未收到模块确认，未推进 stories/cases/执行/Gold 冻结。详见 ../reports/trade-only-continuation-2026-09-19.json。

2026-09-19 范围更正：用户要求单页面应用不处理其他 URL。本轮仅 /trade 页面内交互；此前跨页面模块提案不再请求批准。约束已保存为项目领域参考 83061e4e927db37e31d2121baba10d36e2a14e37691063e9a16d3c92952f96e9，新运行绑定此版本，旧运行材料不变。原 6 条 Gold 候选归档，新候选 ui-trade-20260919 仅保留 2 条 trade 观察项，未复核、未划分有效留出集，不能冻结评分。当前只落地范围指令与数据收窄，未实现浏览器 URL 强制拦截，也尚未重新生成单页面模块提案。

2026-09-19 新项目真实实走发现：Codex 被旧 managed planner 守卫拒绝、探索重试发生 source revision 冲突；Claude Code 在模块未冻结时仍写故事。已接通 Codex Web/native 入口、来源父版本写入，并在服务端 begin/write 双路径拦截未冻结模块；Claude 违规运行已取消，保留失败。Gold 历史两套数据归档只读，新建项目候选入口落地；记分板隔离项目/研究历史并单列诊断；新增同材料、已定稿原生产物的 dev 比较，不能替代原生 A/B 启动器。

新项目 prj-mu7sqa2s-1001；Codex run-b07a11a5-3ea7-4e8b-9e02-28b3c7c187e7 在模块冻结等待用户，Gold ui-acceptance-20260919 为 6 条未复核候选。未完成执行或有效评测。真实 MCP 两运行时各 46 工具且 schema hash 一致；相同未冻结 Gold 均返回拒绝。验证 1438 项 Vitest、MCP Node 4、hooks 23、scripts 7；typecheck/build/drift/domain-neutral 通过，host parity 184/184（11 UI 专属），独立准入 6/6。最后 UI 调整后重跑 app 类型检查/构建通过。详见 [实走报告](../reports/product-acceptance-2026-09-19.html) 与 [插件原始证据](../reports/host-parity-live-2026-09-19.json)。用户确认具体模块树后才能恢复生成，后续用例批准和 Gold 复核仍需真实人工决定。


2026-09-19 用 Codex 插件与真人操作路径复验，**纠正上一轮交付口径：评测工作台不等于完整评测流程可用**。会话插件连旧 main，页面连预览；旧插件启动返回 ok 但评测列表为空。将当前插件 stdio 显式绑定预览后，两个实验真实返回 gold 缺失 / runtime 不支持。Gold 默认 casegen 冻结只读；领域数据仅 12 条旧草稿、0 留出、无正式 gold，不能代理人工直接冻结。

补上 Gold 范围与前置条件说明、加载错误重试、切换竞态与保存保护、评测跳转指定 Gold 和网关提示；修掉旧 README 指纹不能开启新谱系；set_gold 改为人工专属，冻结先检查来源。Vitest 1430 项、MCP Node 4 项、typecheck、build、drift、domain-neutral 通过，host parity 182/182（11 UI 专属）。真实 MCP 调用和 HTTP 403 已验证，未修改任何 benchmark 数据。主实例仍是旧进程；native runtime / 人工 Gold / 可视化新建流程仍未完成。详见 [插件实走记录](../reports/codex-eval-gold-walkthrough-2026-09-19.md)。

2026-09-19 将自测补强落入产品，分支 `codex/harness-lifecycle-self-audit`。新增实际评测工作台：定义选择、运行前检查、明确阻断原因、启动票据、历史轮询、结果与失败详情；保留显式数据集的单次覆盖检查，并修正窄屏导航。后端拒绝未支持 runtime、缺失流程/数据集/评分节点；失败、超时、缺产物或提示词漂移均不能生成有效 A/B，未知注入和基础设施失败不产生变异得分；交互评测仅使用 development 项，报告标为 diagnostic。人工审核等 7 个动作从宿主工具注册中剔除，伪造分派被拒绝；HostApi 带 agent 来源，服务端不将其视为 local-human。未恢复登录。此来源标记不是针对任意本地客户端的安全沙箱。

验证：1429 项 Vitest（164 文件）及 MCP Node 4 项通过，hooks 23 项、scripts/test 7 项、typecheck、build、drift、domain-neutral、i18n 1979 通过。宿主能力口径校正为 183/183，UI 专属 10 条（新增 7 条是人工决策权限收窄）。独立准入 6/6；其中 R01 明确改为“不支持 runtime 时执行前拒绝”，不是实现 native runtime。浏览器验证真实后端的阻断和移动端；运行→失败展示另用隔离 HTTP fixture 验证。未调用真实模型或被测站，尚无最佳版本结论。详情见 [产品落地记录](../reports/harness-product-implementation-2026-09-19.md)。旧 HTML/JSON 保留为修复前历史证据。

2026-09-19 在 `codex/harness-lifecycle-self-audit` 做架构与功能生命周期自测（基线 `d2e62ce`）。**先说出乎意料的**：现有回归全绿，但面向评测选版的 6 项独立准入检查有 4 项不满足：runtime 未到 graph 执行端、unknown 注入仍计满分、无产物的失败运行仍返回数值比较、规划器宿主审核调用可取得 local-human 来源。最后一项只走工具分派/请求头/权限判定，没有写真实批准。

新增 `server/audit/`、`scripts/harness-readiness.mjs` 和 `pnpm test:harness-readiness`，输出每项观察值、源码哈希与 JSON；缺口未修就返回 1，不用 expected-failure 或 skip 隐藏红灯。正常回归与准入自测分开；本轮未改生产逻辑、未调用真实模型或被测站。详细的 16 项功能生命周期（预检、执行、失败恢复、冻结失效、评测反馈）与渐进重构方案见 [HTML 报告](../reports/harness-lifecycle-self-audit-2026-09-19.html)，[机器可读矩阵与结果](../reports/harness-lifecycle-self-audit-2026-09-19.json)。

验证：`pnpm test` 的 Vitest 163 文件 / 1419 项通过，另有 MCP Node 测试 4 项；hooks 23 项、scripts/test 7 项通过；typecheck、build、drift、host-parity 190/190、domain-neutral 通过。独立准入检查 **4 failed / 2 passed，blocked**。这些是基础设施与契约自测，不是模型 benchmark；领域冻结任务、真实模型配对、真实 UI 稳定性与通用版本晋升仍待补齐。下一切片优先修准入有效性和 planner/operator 能力边界，不恢复已取消的 ReviewerSession，不自动解冻整条研究线。

2026-09-19 刷新 CLAUDE.md，让它和现状对得上。过时的四处：①「新建公开仓库、不带提交历史，现有私有仓库原样保留」——实际是代码就在公开仓库 `zyonlab/TestPilot` 上、带完整历史、改动走 PR，那条计划没执行；②「`git push` 仍未授权」——规则仍然是当轮确认，但措辞像是从没授权过，改成「每次都是用户当轮点的头」；③ 目录速查里的 `fixtures/` 与 `benchmark/` 还是清理前的样子；④ 验收命令少了 `check:i18n` 与 `scripts/test`。

新增一节「人把关的三处，谁也不能替」：写清三个等人的地方、2026-09-19 上线的「驳回必须写理由」（400 `rejection_requires_reason`），以及规划器能绕过冻结与批准这个**没修**的缺口，指向 02 §10-1。另外把 02 加进「先读什么」，并在目录速查里点明规则包契约测试的夹具去了哪里（`packages/harness-testing/test/fixtures/perp-lab-rules.json`，12 条测试逐条断言它的内容）。

2026-09-19 修掉「驳回不带理由」（02 §10-7）。这是挡着反例沉淀的那个堵点：`WorkflowRuns` 的复核面板提交时不传 `note`，而 `proposeFromRejections` 只收理由 ≥4 个字的驳回，于是从工作台驳回多少条，反例候选都是 0。

改了三处，界面和服务端都堵上：`WorkflowRuns` 与 `ProjectReview` 都有「驳回理由」输入框（后者原来藏在折叠的「复核说明」里，现在摊开在操作栏上），理由不足 4 个字时驳回按钮禁用、hover 说明原因；`decideRevisions` 一批里只要有 `rejected` 就要求 `note` ≥4 个字，否则 400 `rejection_requires_reason`——宿主工具 `tp_review.decide` 走的是同一个函数，所以 agent 也绕不过去。批准不受影响。

验证：新增一条测试锁住服务端那道拦截（不写理由、理由只有一个字都要抛）；真浏览器里点过两处界面：只选用例时驳回仍禁用，理由写 1 个字仍禁用，写够 4 个字才可点（批准按钮选中即可用）。全量 typecheck、vitest 六包、hooks 23、drift、host-parity 190/190、domain-neutral、i18n 1927 全绿。

2026-09-18 收掉两处「夹具漏进产品」。用户问 fixtures / evals 里为什么有目标网站相关的东西、会不会伤通用性——查下来红线本身是守住的（`check:domain-neutral` 盯着产品源码与 skill），但确实有两处把仓库夹具暴露给了用户项目：

- `GET /api/materials` 把 `docs/` 与 `fixtures/` 下所有 Markdown 列成「可选材料」，实测返回 172 个文件，其中有 Hyperliquid 的材料。它是自举时代的产物（让 TestPilot 拿自己的文档当材料），对用户项目毫无意义。它的界面 `Workspace.tsx` 早已是死代码，但接口还在，还在宿主入口里映射成 `tp_project.materials`。
- `pack.ts` 的默认 specPath 写死成 `fixtures/sample-spec/acme-portal.md`。内容是合成的、不含领域，但产品代码指向仓库夹具本身就不对。

改了什么：删掉 `server/src/materials.ts`、`/api/materials` 路由、registry 里的 `tp_project.materials`，以及跟着它一起死掉的 `Workspace.tsx`（2021 行）、`Artifacts.tsx`、`MaterialPicker`、`SpecDrawer`、`StoriesDrawer`，共 2456 行前端死代码；host-parity 基线重写为 190/190。`pack.ts` 不再给默认规格路径，没给就让图跑到 `source.spec` 时明说缺输入。

**出乎意料的**：`chat.test.ts` 有一条挂了。它断言「草稿丢掉 `docs.path` 要被说出来」——那条警告之所以存在，正是因为注册图里有那个默认路径。默认值拿掉后，该警告改成落在 `spec.*` 上，测试断言同步改了，意图（丢参数必须被说出来）没变。

验证：typecheck、vitest 六个包全绿、hooks 23、scripts/test 7、drift、host-parity 190/190、domain-neutral、i18n 1924、`pnpm build` 通过。

2026-09-18 清掉非 Hyperliquid 的数据集。**先写出乎意料的**：以为都是无人引用的旧数据，实际有三处是活的——① `fixtures/perp-lab/rules.json` 不只是那个本地 mock 交易所的数据，`domain-rule-pack` 与 `domain-report-model` 的 12 条测试逐条断言了它里面的目标与规则（换成 Hyperliquid 那份规则包后当场挂 12 条），所以把这一个 JSON 留成测试夹具 `packages/harness-testing/test/fixtures/perp-lab-rules.json`，SUT 本体删掉；② `fixtures/mock-spec` 是 `pack.ts` 的默认 specPath 和 `evals.ts` 默认 goldPath，还撑着 G1 的假模型端到端测试，改名 `fixtures/sample-spec` 留下（它是合成的 Acme Portal，不是任何真实被测对象）；③ 一度把默认 specPath 指到 Hyperliquid 的材料，`check:domain-neutral` 立刻拦下——产品源码不许写死领域内容，这条规则在这里正好生效。

删了：`benchmark/binance-futures`（`cases/` 三条 eval-case 夹具移到 `fixtures/eval-cases`）、`benchmark/local-perp`、`fixtures/{benchmark,recordings,perp-lab,tier4-demo,self-test}`、6 份依赖它们的 evals（只剩 `domain-perp` 与 `runtime-compare`）、4 个 perp-lab 验证脚本与 `run-g1.ts`、`scripts/test/perp-lab.test.mjs`。改了：`audit.ts` 默认能力 `binance-futures` → `casegen`，`evals.ts` 默认 gold → `benchmark/casegen/gold.json`，5 个测试的夹具路径，`stop-p0` 的 hook 夹具移到 `scripts/test/fixtures/`，`testpilot-explore` 的 SKILL 去掉 binance 那段（版本跳 2026-09-18.1，两份宿主副本已重建）。本机：删掉「Vikunja 通用性验证」项目与 `~/.testpilot-sut/`。

验证：typecheck、vitest 1400+（6 个包全绿）、hooks 23、scripts/test 7、drift、host-parity 191/191、domain-neutral、i18n 1924、replay 逐位相同。

2026-09-18 README 改成一条能一眼看完的主线。之前是「流程图 + 能力表 + 截图堆在一处」，图和截图分家，读的人要自己拼。现在把 02 的流程图、界面截图和真跑截图融进 README：全流程图标出三个等人的地方（冻结模块树、复核、回归候选），下面八小节按 ①～⑧ 走一遍，每节配那一步的截图和它产出什么、被什么挡住；新增「系统组成」图（浏览器 / API 与两个库 / 规划宿主与 MCP / runner / 被测站点）。「用法 · 从 Web 开始」里重复的六步删掉，改为指回走一遍。中英文同步，11 处图片引用，链接与图片路径都在。

2026-09-18 README 补真跑截图。用户问「为什么没有运行截图」——之前四张全是界面，没有一张是测试真跑在被测站点上的样子。补了三张，都来自 2026-09-16 那次测试网执行里通过的 C-MKT-BOOK-05：执行过程（每步页面截图 + 该步判定）、视觉基线（本次留下的三步截图）、Midscene 报告（断言在测试网页面上通过）。执行过程那张第一次截到的是第 1 步，页面还是导航时的黑屏，而且上方「准备」里带着注入钱包的地址（同一个钱包在主网有真钱），公开仓库不该放，改成从第 2 步截起。同样三张也放进 02 §9。

2026-09-17 补界面截图和 Claude 插件安装说明。**先写出乎意料的**：插件只能从本地 checkout 装，因为生成的插件用 `${CLAUDE_PLUGIN_ROOT}/..` 去引用仓库里的 MCP 入口和 hook 脚本，直接从 GitHub 地址装是跑不起来的。我原以为装成插件会把目录复制进缓存、导致这些相对路径失效。在隔离的 `CLAUDE_CONFIG_DIR` 里实测下来，从本地市场安装时 `CLAUDE_PLUGIN_ROOT` 仍指向 checkout 里的原目录，MCP 显示已连接；已安装插件时再加 `--plugin-dir`，MCP 也只有一份。

改了什么：
- 新增 `.claude-plugin/marketplace.json`，`claude plugin validate .` 通过；
- 中英文 README 第 4 步改为「安装 Claude Code 插件」，给出三种方式（本地插件市场、`--plugin-dir`、工作目录安装），包括更新和卸载命令；README 里加了四张截图的「界面一览」；
- 14 §2.2 同步更新，并更正「模型没有冻结工具」这句错误说法；
- 用 puppeteer 截了 13 张图（1440×900，中文界面，2026-09-16 的 Hyperliquid 测试网运行），放在 `docs/assets/workflow/`，插入 02 的 §5–§9，并新增 §6.1「看用户故事」。回归候选那张没能展开「已决定 3 条」。

验证：`check:drift`、`check:domain-neutral` 通过，`node --test scripts/test/*.test.mjs` 9/9 通过，改动文档里的相对链接和图片路径都存在。

2026-09-17 补齐工作流文档：新增 [02-工作流-横向与纵向](02-工作流-横向与纵向.md)。横向部分有全流程图、各阶段读写表、运行状态机、暂停/续跑/门禁打回，以及还没退役的 `/api/wf/runs` 图系统；纵向部分是新建运行、冻结模块树、复核、编译执行、看报告五张时序图。所有内容按 HEAD `e445d26` 的代码逐条核对过，并从 00 §5、§12，`docs/README.md` 和中英文 README 链过去。

**先写出乎意料的**（读代码发现，**都没修**，详见 02 §10）：

- **规划器在技术上能绕过人工关口。** 规划会话的 MCP 进程无条件注册了宿主工具，`--allowedTools mcp__testpilot__*` 也放行了它们。其中 `tp_stage.freeze_modules`、`tp_review.decide`、`tp_review.decide_candidate` 发请求时不带令牌，服务端就当成 `local-operator`，拦着的只有工具说明里一句「人点头了再调」。这条碰的是「人把关」红线，建议下一步先修。
- **Web 表单默认勾选工作单元，用 spec 来源新建运行必然 400**，报 `work_units_require_product_model`（`workflowOps.ts:62`），而表单里没有导入产品模型的入口。
- **规划器干净停在半路也会被记成 `failed`。** `finish()` 里给 `paused` 留的分支走不到。
- **Claude Code 运行在服务重启后停在 `interrupted`，不会被接回。**
- **g2 路由有两处问题。** 一是不拦 `interrupted`；二是 handler 抛错后，g2 节点一直停在 `running`。
- **从 `WorkflowRuns` 页驳回不带理由**，所以永远不会产生反例候选。
- **执行跑的是自然语言步骤，不是 g2 编出的代码。** g2 代码只用于代码门禁和身份比对。

2026-09-17 面向开源补齐 README 与仓库文档。**先写出乎意料的**：照着 README 跑 `doctor` 会被误报——web 入口一律按 Penguin 检查（Node 24、`TP_PLANNER_*`），而 Web 发起的生成默认由 Claude Code 规划；`testpilot-setup.mjs --help` 直接抛异常堆栈；README 里一条 Midscene 文档链接是 404。

改了什么：`doctor` 的 web 入口改为按 `TP_AGENT_RUNTIME`（不设即 `claude-code`）决定查 `claude` 还是查 Penguin，输出多一个 `webPlanner`；`--help` 与未知命令打印用法（未知命令退出码 2）；`scripts/test/setup.test.mjs` 加一条测试锁住这两件事。README 中英文重写（徽章、项目状态、核心能力、常用配置、安全、贡献、已知限制、致谢，命令都核实过、外链都打开确认过）；新增 `CONTRIBUTING.md`（中英，含九条基本规则与提交前检查）、`SECURITY.md`（中英，范围与威胁模型）、`CODE_OF_CONDUCT.md`（贡献者公约 2.1）、`docs/README.md`（现行文档与历史资料的索引）、`.github` 的 issue 表单与 PR 模板；根 `package.json` 补 description、repository、homepage、bugs、keywords、`engines.node >=22`（仍是 `private: true`，不会被误发到 npm）；10 号文档的 doctor 说明同步。

**还要人决定**：GitHub 的私密漏洞报告没开（`SECURITY.md` 先写成「开一个不带细节的 issue 请求私下联系方式」，开了之后应改成 Security → Report a vulnerability）；仓库没有 description 与 topics；仓库是公开的，而本页与 CLAUDE.md 仍写着「现有私有仓库原样保留」。

2026-09-17 界面微调：工作台节点图与产品模块图改成流程图（`@xyflow/react`，项目里本来就有），抽屉改成右侧可拖宽度并记住。**先写出乎意料的**：
①缩放按钮点了没反应，查了三轮才定位：「适应画布」的副作用依赖 `fitView`，而它的引用随视野变化，每点一次 +/− 就被拉回；内置 `fitView` 属性在节点重新测量后也会再适应一次；带过渡动画的缩放在后台标签页里不推进。现在只在「每个布局第一次测完尺寸」和「从隐藏变可见」时适应，缩放不带动画。
②模块节点点不到：@xyflow 给「不可选、不可拖、没有点击回调」的节点设 `pointer-events: none`；另外复核页每 4 秒轮询一次，每次都重建节点对象，@xyflow 重新测量期间把节点设为 `visibility: hidden`，点击落到背景层。现在给一个空的 `onNodeClick`，并按节点内容（去掉函数）算签名，内容没变就沿用旧数组。
③工作台的抽屉一直挂载着，宽度只在第一次打开时按当时的用途初始化——产物抽屉打开成了运行详情的宽度。现在用途一变就按新用途重读宽度。

改了什么：`FlowCanvas`（点阵底纹、右下角 − / 百分比 / + / 适应画布，滚轮平移、Ctrl/⌘ 滚轮缩放、节点不可拖）、`StageFlow`（8 个阶段两行蛇形排布，走过的连线为绿、正在跑的流动，断点/节点详情/产物入口照旧）、`ModuleFlow`（从左到右的树，父节点居中于子节点，点模块筛选，故事数为 0 时标黄）；`Drawer` 宽度写进 localStorage（按用途分开记，存储不可用时退回内存），「整屏」类抽屉默认也按视口约七成打开、整屏改由标题栏按钮切换，左边缘加了可见抓手、双击恢复默认；工作台原来自写的全屏 `aside` 换成这个 `Drawer`。复核页去掉包住模块图的 288px 限高。浏览器里用真实鼠标核对过：工作台 92% → 64%、模块图 68% → 98%，点「持仓与风控」后复核列表 115 → 33，抽屉拖到 762 后刷新仍是 762、产物抽屉按自己的默认 1105 打开。

**顺带发现**：`origin`（`zyonlab/TestPilot`）是**公开**仓库，而本页与 CLAUDE.md 里写的是「现有私有仓库原样保留」。推送前核对过：`docs/v3/evidence/` 没被跟踪；公开的 main 上已有同类文档与合约地址；这次没有新增钱包地址或密钥。「私有仓库」这个说法需要用户确认后改正。

2026-09-16 对照一份 QA Agent 岗位要求补了三块：生成内容的判据、失败沉淀为回归候选、一跑的自动归因。**先写出乎意料的**：
①我第一版的回归候选只在批次状态为 `failed` 时触发，而一批里只要有一条环境失败，批次就记成 `infra_error`——同批里真正判定失败的用例会被整批漏掉，测试当场抓到。
②归因报表第一版把「没有基线的判定失败」一律归到**产品**。拿测试网真跑（Hyperliquid，9 条已批准用例，4 过 5 挂）一对，4 条判定失败**一条都不是产品问题**：两条是 tier 3 判官看错区域、选错行（同一屏上程序判据明明找到了「Spread」），一条是程序判据在「Funding / Countdown」旁读到了资金费率而不是倒计时，一条带着环境错误标记且前提不成立（页面写着没有持仓）。于是改成：判官判挂的先归到模型（也可能是用例、产品），只有程序判据判挂的才先归到产品。报表与候选对「带环境错误标记」的口径也统一了。
③第 1 项的名字不能叫 rubric：`benchmark/*/rubric/` 是受保护目录，读取规则会拦这个词，所以判据叫 `judge`。

**改了什么**：
- **`judge` 判据**（`exec/oracle.ts` 加一支与纯函数 `aggregateJudge`，采样在新文件 `exec/judge.ts`，每次轮换条件的提问顺序——执行模型温度 0.1，不轮换的话重复问几乎总是同一个答案）。执行器、导出工程（`checkJudge`，采样器原样复制进去）、受限解码 schema、占位剥离、单元契约、前端展示都接上了。门禁加 `judge-spec`（条件含糊、及格线超过采样数记 warn，采样少于 3 记 info），并堵了一个洞：只带 `judge` 的 tier 1 用例以前会被当作有机器判据放过。单元契约里原来那行 `{"kind":"api",…}` 写法换掉了——它紧挨着「永远不要对接口下判断」。生成提示词 `CASES_STABLE` 一条规则改写，`testpilot-design` 跳到 `2026-09-16.2`，两份宿主副本已重建。
- **回归候选**（`server/src/regressionCandidates.ts`）：执行收尾与复核接口自动触发；同一版用例只出一个候选，决定过的不再冒出来；决定必须是人；批准后进 `regression_suite`。路由 4 条、宿主工具 4 个，`regression-candidate/` 成为保留前缀。
- **归因报表**（`server/src/runReport.ts`）：`measure-run.ts` 的四节点算法搬进来，脚本改为调用它；新增按 `message.id` 去重的宿主流水解析、执行与基线汇总、固定归因规则。`checkModulePlan` 的调用也改成与定稿时同一种（带故事），原脚本传错了参数，一直靠字段名碰巧对上。

**界面**（同日补）：运行详情里加了「归因报表」面板（六层排序、每条信号与依据、四个节点的关键指标、宿主用量、执行归属）和「回归候选」面板（证据、判据原话与判官理由、写判断后批准或驳回、从最近一次执行收集、项目回归集）；执行报告里 judge 判据显示采样统计（几次全部成立、每条成立几次、是否分歧）。文案三语。浏览器里对 Hyperliquid 那一跑核对过两个面板；**没有真实数据的两处没在浏览器里看过**：待决定候选的批准/驳回按钮（当时没有待决定的候选），以及 judge 采样统计（还没有带 judge 判据的执行）。

**执行详情与文本用例详情**（同日再补）：
- **先写出乎意料的**：工作流执行每条用例都会写一条运行记录、立视觉与性能基线，但两边没连起来——执行产物里不记运行记录 id，运行记录上也从来没截 Midscene 报告（报告接口对工作流运行一律 404）。现在执行刚结束就截报告，产物里写 `runRecordId`；老执行在读明细时按「板上用例 id + 运行 + 开始时间」对回运行记录，报告按**创建时间**落在「这条开始～下一条开始」之间补截（不能取「最新的一个」，后面的用例已经跑过了）。测试网那次执行补截了 6 份，逐份核对，每份里只出现它自己那条用例的断言原文；没调过模型的 3 条用例本来就没有报告，界面上写明原因。
- 新接口 `GET …/:runId/executions/detail/:executionId`（`server/src/executionDetail.ts`，宿主工具 `tp_execution.detail`）。
- 界面：执行报告里每条用例改为可展开的六个标签——概览（失败原因、入口、判定时页面、模型调用次数、判据/视觉/性能摘要、耗时分段条）、执行过程（按准备/步骤/判定/清理切段，每步配截图）、判据（由谁判、步骤前是否已成立、judge 采样统计）、视觉基线、性能、原始数据；右上角是 Midscene 报告与「复核此用例」。
- 文本用例：新组件 `TextCaseDetail` 取代复核页、运行详情复核列表与产物查看里的三套渲染。原来复核页只画标题/前置/步骤/预期，判据是一段 JSON；现在按「为什么测 → 覆盖（验收准则原文）→ 前置与测试数据 → 步骤与判定（断言挂在它该判的那一步后面，标明程序判还是模型判）→ 清理 → 设计依据 → 就绪度 → 出处 → 这条用例的门禁意见」展示。

**真实验证**：
- `eval-judge.ts --real`：golden 9 条 × 2 遍，**18/18 判对**，两遍一致，54 次采样零分歧，每条中位 9.2 秒。
- 测试网执行 `exec-09f2e9c0…`（9 条）：自动出 3 条缺陷候选（定位失败 1 条、带环境标记 1 条按设计跳过）。我按用户授权逐条看证据后**全部驳回**，理由写在各自的 note 里；回归集保持为空。没有为了演示去批准。
- 全量：typecheck 0；测试 server 487、harness-testing 589、harness-core 226、testpilot-mcp 119、apps 5 全过，hooks 23 过；check:drift / host-parity（190/190）/ domain-neutral / i18n 全绿；replay 逐位相同。

2026-09-16 文档对着代码整理了一遍。**先写出乎意料的**：`docs/v3/` 下 27 篇文档里，只有 5 篇还在描述现状；其余是实验报告、任务台账、设计提案和交接日志，却都顶着「最新」「当前」的字样。连本页之前的开头几节都写着早已不存在的分支与 HEAD，还把已经冻结的 Gold、自进化列为下一步。README 仍在介绍截图和 Dapp，还教人分叉主网。

改了什么：23 篇过时文档与旧的交接日志整份挪进 `history/`（交接日志改名为 `09-交接日志-至2026-09-16.md`）；代码、测试、脚本与 skill 里指向它们的路径同步改掉；00 架构、01 数据契约、10 安装、14 宿主接入对着代码重写或核实；README 中英文重写，不再放截图；日文 README 与截图目录删除；根目录的 `Claude outputs/` 与 `docs/build-log/` 并入 `docs/archive/`。`testpilot-design` 的 REFERENCE.md 里有一个路径跟着改了，因此跳版本到 `2026-09-16.1` 并重建宿主副本。顺带修了一处我自己留下的不一致：上一轮把词表放回了每个单元，起跑话术（`server/src/runtime/skill-launch.ts`）却还在说「claim_unit 从不重复词表与角色」，现在改成只有领域参考不重发。CLAUDE.md 里「`pnpm test` 含 hook 子进程测试」也不对，hook 测试在 `pnpm test:hooks`。

验证：typecheck 0；测试 server 472、harness-testing 576、harness-core 226、testpilot-mcp 119、apps 5 全过，hooks 23 过；check:drift / host-parity（185/185）/ domain-neutral / i18n 全绿；现行文档（README 中英、CLAUDE.md、docs/v3 下 5 篇）死链 0。文档正文只做了静态核实（路径、导出名、script 与环境变量都 grep 过），安装器与 doctor 以外的命令没有实际跑。

更早的记录：[交接日志（至 2026-09-16）](history/09-交接日志-至2026-09-16.md)、[交接日志（至 2026-09-09）](history/09-handoff-through-2026-09-09-2104.md)。
